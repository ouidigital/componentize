---
/**
 * NotFound-2501 — generated from CodeStitch stitch #2501.
 * Source: https://codestitch.app/app/dashboard/stitches/2501
 * Target: Advanced Astro i18n @ 5de7f5fe
 *
 * Draft — needs attention before shipping:
 *   - The fr locale file holds the English copy — translate it before this goes live.
 */

// Utils
import { getLocaleFromUrl } from "@js/localeUtils";
import { useTranslations, getLocalizedRoute } from "@js/translationUtils";

// Components
import { Picture } from "astro:assets";

// Images
import real6 from "@assets/images/not-found/real6-cdf41f6c.jpg";
import rectangle from "@assets/images/not-found/rectangle-a98d1dc1.svg";

const locale = getLocaleFromUrl(Astro.url);
const t = useTranslations(locale);

const rectangleBg = `url("${rectangle.src}")`;
---

<!-- ============================================ -->
<!--                     404                      -->
<!-- ============================================ -->

<section id="not-found-2501">
	<div class="cs-container">
		<div class="cs-content">
			<span class="cs-topper">{t("notFound2501:topper")}</span>
			<h2 class="cs-title">{t("notFound2501:title")}</h2>
			<p class="cs-text">{t("notFound2501:text")}</p>
			<div class="cs-button-box">
				<a href={getLocalizedRoute(locale, "/about/")} class="cs-button-solid">{t("notFound2501:buttonSolids.0.button")}</a>
				<a href={getLocalizedRoute(locale, "/about/")} class="cs-button-solid">{t("notFound2501:buttonSolids.1.button")}</a>
			</div>
		</div>
	</div>
	<Picture
		src={real6}
		alt={t("notFound2501:picture.alt")}
		formats={["avif", "webp"]}
		width="1920"
		height="104"
		pictureAttributes={{ class: "cs-background" }}
	/>
</section>

<style define:vars={{ rectangleBg }}>

	/*-- -------------------------- -->
	<---           404              -->
	<--- -------------------------- -*/

	/* Mobile */
	@media only screen and (min-width: 0rem) {
	  #not-found-2501 {
	    height: 100dvh;
	    padding: 1rem;
	    overflow: hidden;
	    position: relative;
	  }
	  #not-found-2501 .cs-container {
	    width: 100%;
	    height: 100%;
	    padding: 1rem;
	    background-color: #f7f7f7;
	    overflow: hidden;
	    display: flex;
	    flex-direction: column;
	    justify-content: center;
	    align-items: center;
	    position: relative;
	    z-index: 1;
	  }
	  #not-found-2501 .cs-container:before {
	    content: "";
	    width: 100%;
	    height: 100%;
	    background-image: var(--rectangleBg);
	    background-position: center;
	    background-size: auto;
	    background-repeat: repeat;
	    opacity: 0.4;
	    display: block;
	    position: absolute;
	    top: 0;
	    left: 0;
	    z-index: -1;
	  }
	  #not-found-2501 .cs-content {
	    /* set text align to left if content needs to be left aligned */
	    text-align: center;
	    width: 100%;
	    max-width: 80rem;
	    display: flex;
	    flex-direction: column;
	    /* centers content horizontally, set to flex-start to left align */
	    align-items: center;
	    position: relative;
	    z-index: 3;
	  }
	  #not-found-2501 .cs-topper {
	    font-style: italic;
	    font-size: 1.5625rem;
	    text-transform: initial;
	    letter-spacing: 0;
	    margin-bottom: 1rem;
	    color: var(--headerColor);
	  }
	  #not-found-2501 .cs-title {
	    font-size: clamp(2.4375rem, 6vw, 3.8125rem);
	    margin-bottom: 2rem;
	  }
	  #not-found-2501 .cs-text {
	    max-width: 30ch;
	    margin-bottom: 4rem;
	  }
	  #not-found-2501 .cs-button-box {
	    display: flex;
	    justify-content: center;
	    flex-wrap: wrap;
	    gap: clamp(1.25rem, 3vw, 2.25rem);
	  }
	  #not-found-2501 .cs-button-solid {
	    font-size: 1rem;
	    font-weight: 700;
	    /* 46px - 56px */
	    line-height: clamp(2.875em, 5.5vw, 3.5em);
	    text-align: center;
	    text-decoration: none;
	    min-width: 9.375rem;
	    margin: 0;
	    /* prevents padding from adding to the width */
	    box-sizing: border-box;
	    padding: 0 1.5rem;
	    background-color: var(--secondary);
	    overflow: hidden;
	    color: #fff;
	    border: none;
	    border-radius: 0.25rem;
	    display: inline-block;
	    position: relative;
	    z-index: 1;
	    transition: color 0.3s;
	  }
	  #not-found-2501 .cs-button-solid:before {
	    content: "";
	    width: 0;
	    height: 100%;
	    background: #000;
	    opacity: 1;
	    position: absolute;
	    top: 0;
	    left: 0;
	    z-index: -1;
	    transition: width 0.3s;
	  }
	  #not-found-2501 .cs-button-solid:hover {
	    color: #fff;
	  }
	  #not-found-2501 .cs-button-solid:hover:before {
	    width: 100%;
	  }
	  #not-found-2501 .cs-background {
	    width: 100%;
	    height: 100%;
	    display: block;
	    position: absolute;
	    top: 0;
	    left: 0;
	  }
	  #not-found-2501 .cs-background img {
	    width: 100%;
	    height: 100%;
	    object-fit: cover;
	    position: absolute;
	    top: 0;
	    left: 0;
	  }
	}
	/* Dark Mode */
	@media only screen and (min-width: 0rem) {
	  body.dark-mode #not-found-2501 .cs-container {
	    background-color: var(--dark);
	  }
	  body.dark-mode #not-found-2501 .cs-container:before {
	    opacity: 0.1;
	  }
	  body.dark-mode #not-found-2501 .cs-topper,
	  body.dark-mode #not-found-2501 .cs-title,
	  body.dark-mode #not-found-2501 .cs-text {
	    color: var(--bodyTextColorWhite);
	  }
	  body.dark-mode #not-found-2501 .cs-text {
	    opacity: 0.8;
	  }
	}
</style>
